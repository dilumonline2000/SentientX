/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["randomuser.me"], // Use 'domains' instead of 'remotePatterns'
    },
  };
  
  export default nextConfig;
  